# AGENTS.md — 开发协作速查

> 给后续开发者与 AI 代理的实操指南。决策一律以 `docs/00-决策记录-开放问题拍板-v1.0.md`（D-01~D-20）为锚点；本文只讲「怎么跑起来、哪些坑不能踩」。

## 环境

- **Flutter SDK 内置**：`.tooling/flutter/bin`（3.44.8 / Dart 3.12.2），不要依赖系统 Flutter。所有命令先 `export PATH="$PWD/.tooling/flutter/bin:$PATH"`。
- **Android**：需 JDK 17+。系统 Java 版本不够时用 Android Studio 自带 JBR：
  `export JAVA_HOME="/Applications/Android Studio.app/Contents/jbr/Contents/Home"`。
- **国内网络**：Android 依赖下载慢/卡死时 `export USE_CN_MIRRORS=1`（阿里云镜像）。CI（海外 runner）严禁开启。
- **iOS 真机**：Xcode → Settings → Accounts 需登录 Apple ID；项目签名团队 `L35RLT89XN`（注：实际现行团队 CCTFP9X3SW "jing chang"，bundleId com.jingchang.eatwise）；部署目标 iOS 15.0。
- **iOS 装机必须带生产地址**：`ApiConfig` 默认 dev=localhost（真机上 localhost 是手机自己，全站网络请求必失败——症状：设置页账号「点击重试」+社区「网络连接失败」）。真机 release 构建命令固定 `flutter build ios --release --dart-define=API_BASE_URL=https://wcg.polin.tech:8443/v1`（局域网调试用 `http://<Mac局域网IP>:3000/v1`）。安卓无此坑（CI 走仓库变量 API_BASE_URL）。
- **iOS 构建（2026-09-18 起 CocoaPods 回归）**：`open_filex` 无 SPM 支持，Pods 重新启用（仅此一个 pod）。Podfile 必须保持 `platform :ios, '15.0'` + post_install 强制 IPHONEOS_DEPLOYMENT_TARGET=15.0——低版本会造成 release 构建/签名静默失败（踩过）。新增只支持 CocoaPods 的插件时：pod install → 真机构建验证 → 提交 Podfile/Podfile.lock/pbxproj/workspace（Pods/ 目录本身 gitignored）。

## 客户端（eatwise_app/）

```bash
flutter pub get
dart run slang                  # i18n 生成（唯一正确方式）
dart run build_runner build     # drift 生成
dart analyze                    # 门禁：零 issue（含 info）
flutter test                    # 门禁：全绿
```

硬性规则：

- **禁止 `flutter analyze`**：中文路径下 SDK bug 会崩，用 `dart analyze`。
- **不要装回 `slang_build_runner`**：它不读 `slang.yaml`，会让 build_runner 整体失败。i18n 只有 `i18n/strings_zh-CN.i18n.json` / `strings_en.i18n.json` 两个文件（namespaces:false，多文件互相覆盖）。
- **代码一致性**：提交前 `dart format .` + `dart analyze` 零 issue + `flutter test` 全绿。CI 有 Android/iOS 双端构建门禁，本地改原生配置后至少跑 `flutter build apk --debug`。
- **分层**：feature-first（presentation/application/domain/infrastructure），Token 走 `lib/core/theme/` ThemeExtension，文案一律 i18n key 禁止硬编码。
- **领域逻辑纯函数化**：计时/营养/streak 等纯 Dart 可测，UI 只做接线。
- **端侧小模型 AI 估算（2026-09-15 核心层入库）**：`lib/core/llm/ondevice/`——Gemma4-E2B `.litertlm`（2.41GB，按需下载）。依赖 `flutter_gemma` + `flutter_gemma_litertlm` 必须成对（core 无引擎）；安装必须显式 `ModelFileType.litertlm`；`maxTokens` 是上下文窗口、限输出用 createChat `maxOutputTokens`。下载自管（OnDeviceModelManager：双源选源/Range 续传/字节+LITERTLM 魔数双校验），**不要用** flutter_gemma 的 fromNetwork 下载（不续传、残留孤儿分片）。估算结果带 sanity-clamp 存疑标记，只能作食物库未命中兜底。iOS 真机需 Xcode 登录 Apple ID；首次加载生成 ~0.75GiB XNNPACK cache 属正常，清理策略要保留。视觉推理（2026-09-16 拍照识别接通）：`load(enableVision: true)` + `inferWithImage`（getActiveModel supportImage → LiteRT-LM enableVision，视觉编码器固定 CPU；supportImage=false 时插件静默丢图，网关已显式拦截），服务在 `features/record/recognition/data/ondevice_food_recognition_service.dart`，识别名必须映射回食物库（未命中降级手动搜索），模型估值只作存疑判定不入账。音频推理（2026-09-19 端侧 ASR 接通）：`load(enableAudio: true)` + `inferWithAudio`（supportAudio → LiteRT-LM enableAudio，litertlm FFI 全链路支持；supportAudio=false 时插件静默拒音频，网关同款显式拦截）；录音用 `record` 7.1.1 插件（16kHz 单声道 PCM16 裸数据，Dart 层封 WAV 头，见 `features/record/recognition/domain/ondevice_asr_logic.dart`）；系统 ASR 不可用（无 GMS）时语音录入走端侧录音转写面板。⚠️ **E2B .litertlm 是否内置音频塔未实证**：若模型不含音频编码器，supportAudio 加载/推理会失败，需真机验证；失败时服务返回 null，语音入口回落降级卡。⚠️ **语音流程转写结果出口（v1.12.9 修）**：`startVoiceInput` 的 systemAsrBroken 直达分支、`_showVoiceEngineGuide`（引导内下载→录音）、听写面板逃生舱三处都必须接住 `VoiceTranscript` 并进 `_handleTranscript`——曾丢弃返回值致无 GMS 安卓机「点完成无检测无记录」（iOS 走系统 ASR 分支不触发）；speech_to_text 7.4.0 的 Android `stop()` 每分支同步 result.success，不存在挂起，别再往那个方向查。
- **食物详情页（阶段 E，2026-09-17 入库）**：搜索结果点击 → `features/record/presentation/food_detail_sheet.dart` 底部弹层（热量大字卡 + 三大营养素**供能比例**三圆环（4/4/9 换算，带 2.25 倍人话注释，非重量比例）+ 红绿灯徽标 + 明细折叠区 + 份量入账）。红绿灯复用 D-05 `classifyVerdict`（p = 每100g 值 ÷ 当日目标 × 100），**聚合只取高侧落区**（redLow/yellowLow 对单个食物不构成警告，红色只表警告）；已知局限：完成率阈值对每100g 食物区分度弱，徽标绝大多数为绿，按热量密度的食物分级待规格新增 + 营养背书。纯函数在 `features/record/domain/`（`macro_energy.dart` / `food_signal.dart`）；弹层确认走 `_openFoodDetail` 回填既有 `_confirm` 链路（乐观更新 + D-11 撤销 + 埋点不变）。份量双轨（口语化单位）未做——食物库无份量单位数据。薄荷走查收尾（2026-09-18）：弹层加「数据有误？」纠错入口（`startFoodCorrectionFlow`，预填当前值提交 `POST /v1/foods/:id/correction`，kind=correction 入同一审核池，管理台原值 vs 建议值对照，approve 应用建议值到共享食物行；FoodCandidate 增 `suggestion` JSON 列）+ 餐次 chips；记录页今日记录按餐次分组（FoodEntries 增 `mealType` 可空列，schema v8，纯本地不上行；智能预判 `features/record/domain/meal_type.dart`：5-10 早 / 10-15 午 / 15-21 晚 / 其余加餐，历史无餐次归「其他」）。
- **占位食物行补名（2026-09-23 走查 v1.13.18「记录页显示 cf_223767c7」）**：下行 change 载荷无食物名称（只有营养快照），iOS 缺 cf_ 行的原因是**自定义食物无下行通道、仅搜索时按需缓存**（非 seed 裁剪——removedIds 只删未被引用的内置行，cf_ 个人库行不受 seed 管治）。修复（纯客户端，服务端零改动零部署）：占位行特征=名称==id 双列（`FoodDao.placeholderRows()` 裸 SQL 列对列比较）；`RemoteRecordSync.backfillPlaceholderFoods` 经既有 `POST /foods/batch-get`（≤200/批，需登录）补真名/真营养/翻正 isCustom，命中 upsert 幂等、查不到保留 id 标记下轮再查；挂 RecordSyncEngine syncNow 下行完成后（每轮全量扫存量 = 升级自愈），补名成功经 `onFoodsBackfilled` 回调失效 `entryFoodProvider`+`recordFoodSearchProvider`。展示兜底「未知食物」（i18n `record.unknownFood`）：`domain/placeholder_food.dart` 的 `displayFoodName`，覆盖今日记录/详情页标题（id 小字可查）/搜索结果。
- **管理员移动端删除食品（2026-09-23 入库）**：服务端 `DELETE /v1/moderation/foods/:id`（`src/food/moderation-foods.controller.ts`，用户 JWT + UserAdminGuard，匿名 401/普通用户 403）与管理台 `/v1/admin/foods` 删除**共用 `FoodService.adminDeleteFood`**（软删任意来源行 + 跨用户级联 tombstone，pending 候选 409/缺行 404，不落第二份）。客户端：FoodDetailSheet 动作行按 `userMeProvider.role=='admin'`（审批中心同 provider）对任意食品显「删除（管理员）」（与 owner 删除互斥：自定义 owner 行仍走原动作行）；确认弹窗明示级联 → `deleteFoodAsAdmin`（`features/moderation/application/admin_food_delete.dart`：远端删 → 本机记录物理删+聚合重算+本地食物行删（共享行同删防幽灵搜索）+ `entryFoodProvider`/`recordFoodSearchProvider` 失效 + syncNow）→ snackbar 带服务端 deletedEntries。`ModerationRemote.deleteFood` 返回级联条数。i18n `record.customFood.adminDelete*`。**404 友好（同日走查 v1.13.18）**：管理台手工软删过的行本地滞留，点删除曾弹原始「资源不存在」——`AdminFoodDeleteResult.alreadyGone` 按已删除处理（照样本地直清 + 「已从本地移除」提示）。**幽灵共享行核验（同日）**：`ContributionReviewSync` 对非自定义幽灵行（带徽标但不在「我的贡献」集）原只清徽标不删行——现在经 `existingFoodIdsFn`（POST /foods/batch-get 命中集，生产接 apiDioProvider，仅本轮有幽灵才调）核验服务端存续，查无 → `_removeSharedGhost` 整行移除（引用记录物理删+聚合重算，静默无通知）；占位行（名称==id）显式排除（占位回查要靠它补名，绝不能被 ghost 删）；核验失败（离线）只清徽标下轮重试。
- **弹层按钮常驻统一骨架（2026-09-23 走查收口）**：华为小屏+大字体+键盘下「确认按钮被顶出屏幕」系列问题的统一口径——`lib/core/widgets/app_bottom_sheet.dart`（封顶 (屏高−键盘)×90% + 内容 Flexible 内滚 + **动作按钮固定滚动区外常驻** + SafeArea/viewInsets 内置）。已接入：食物详情/拍照明细确认/自定义食物/条码补录/记运动/运动截图确认/断食窗口编辑七个弹层；记录页结果卡 `_SelectedFoodCard` 同构（卡内 Flexible 内滚 + 按钮常驻卡底，外层 Flexible(flex:3) 占剩余空间——v1.13.9 的 ConstrainedBox(90%) 在键盘下仍与非 flex 兄弟叠加溢出）。**两个坑**：① 调用点旧的外层 `Padding(viewInsets)` 会与骨架双倍扣高（360x640+键盘实测扣到 40px），一律删除；② 食物详情千卡/千焦行窄屏横向溢出，kj 文本走 Expanded+FittedBox(scaleDown)。钉死测试：`test/core/widgets/app_bottom_sheet_test.dart`（360x640+scaler1.3+键盘三条件）+ 各弹层既有测试补小屏键盘用例。其余弹层结论：AlertDialog 系（内容自滚+actions 常驻）、里程碑分享/语音面板（Flexible/Expanded 封顶）、语音模型下载/来源选择（小内容）均 OK 未动。**选中-确认 UX 走查（同日二轮）**：份量必填引导统一为「空/非法份量 → 确认按钮禁用 + 行内 `请输入大于 0 的份量` helper」（食物详情弹层 + 结果卡同口径，替代旧「按钮看似可点、点了才弹错误吐司」截停链）；确认后回连记态语义=搜索清空 + 空查询展示「今日记录」分组列表（薄荷式，勿改回隐藏列表——v1.13.9 教训）。
- **重装登录恢复两连修（2026-09-19 走查）**：① 引导重弹根因——`_applySession` 只写内存 OnboardingGate 不持久化，杀进程重开冷启动从 SharedPreferences 读 false 再进引导；修复=服务端 onboardingStatus（completed/skipped，登录/注册响应已带）下行时**内存门禁+`OnboardingStore.markOnboardingCompleted()` 双写**（AuthController 增 `onboardingStore` 可选参，auth_providers 装配，时序先于路由翻转）。②「有记录但首页不显示」根因——`/sync/pull` 下行入库（`RemoteRecordSync._applyChange`）从不重算 `daily_nutrition_caches`（首页今日汇总/信号卡唯一数据源，缓存只在本地入账/撤销时重算）；修复=`_pullPages` 收集受影响归属日统一 `recomputeDailyNutrition`（含 tombstone 软删回落）。③ ②的存量盲区（v1.12.5 走查）：旧版本已下行记录游标已越过，pull 无新变更 → 缓存永远缺失不自愈（数据页近 7 日趋势 `weeklyTrendProvider` 读同一缓存表，一并假空）；修复=`DailyNutritionCacheRepair`（`features/record/data/daily_nutrition_cache_repair.dart`：缓存缺失或 entries.updatedAtUtc 新于 cache 即重算，幂等）挂 RecordSyncEngine finally 路径（**不依赖在线/有无变更**）——每用户首次全量回填（prefs `daily_cache_backfill_v1_<userId>` 标记），之后每次 syncNow 只扫近 7 天廉价窗口。
- **手动记运动（2026-09-19 入库）**：无 GMS 设备（鸿蒙等 Health Connect 不可用）手动兜底。MET 系数表 + kcal 估算（MET × 体重kg × 时长h，体重缺省 60kg 兜底并 UI 标注）+ 系统/手动合并纯函数（`mergeBurnKcal` / `mergeSteps` / `estimateKcalFromStepsWalk`：体重 × 距离 × 1.036，距离缺省 步数 × 0.75m 步幅，常量〔待营养背书〕）在 `features/health/domain/exercise_types.dart`；类型表 17 种。drift `exercise_logs`（schema **v12**；v10 补 `source`，v11 补 `steps` 步数持久化，v12 补两态同步字段 clientRequestId/serverId/syncState/deleted——历史行幂等键回填=localId）。**服务端上行（同日拍板）**：prisma `exercise_logs` 表（migration 20260919140000，随下次部署 `prisma migrate deploy`），复用 /sync/push|pull 体系（entity=exerciseLog，轻量两态仅 create/delete 无 update——运动记录无编辑场景，改=删了重记；pull 随行 `exerciseLogChanges`，口径与 waterLog 完全一致；U3 导出/U5 清除已含）。客户端 `RemoteExerciseLogSync`（`features/health/data/`）挂 RecordSyncEngine（仅登录态上行，匿名本地 pending 保留；删除=未上行物理删/已上行 tombstone 上行 delete op；下行不覆盖本地 pending）。**合规边界不变**：系统健康数据（HealthKit/Health Connect 实时步数）仍不落盘不上行，仅用户主动录入/截图确认的记录同步。仓储/Provider 在 `features/health/data|application/exercise_log_*`；记录页快捷操作区第五入口「记运动」→ 弹层 `exercise_log_sheet.dart`（走路可按步数录入自动换算热量）。首页预算行/数据页消耗环=系统+手动合计（mergeBurnKcal），数据页步数=系统步数+当日落库合计（mergeSteps）；unsupported 无步数显「—」+引导。**截图导入**：弹层「拍照识别/相册导入」→ 端侧视觉严格 JSON（`exercise_screenshot_logic.dart`）→ 可编辑确认弹层落库（source='screenshot'，步数随记录）。**展示缺项走查**：里程碑分享卡「80% 的伙伴」虚假分位已改自我鼓励口径。
- 同日运动记录「更新 or 新增」（v1.13.0）：华为等截图是全天汇总，同日重复导入旧逻辑只追加导致消耗重复累计。两个保存入口（手动弹层 + 截图确认弹层）经 `features/health/presentation/exercise_save_conflict.dart` `saveExerciseWithConflict`：当日已有记录（`repo.todayLogs()`，与 add 同 `_clock` 口径）→ 三选弹窗（再加一条/替换今天记录/取消=留在弹层）。替换 = `ExerciseLogRepository.replaceToday`（旧行逐条两态口径删 + 新行 add = delete ops + create op，**服务端零改动**，协议本就无 update）；D-11 撤销走 `undoReplace`（删新行 + 快照旧行换新 clientRequestId 重新 pending 上行，行身份变化可接受）；弹层返回协议 `ExerciseSaveResult{saved, replaced}`。
- **APK 分发链路（2026-09-19 终版，仓库 public）**：**更新检测直连 GitHub**（`UpdateChecker` 裸 Dio 调 `api.github.com/repos/chunguangwei/EatWise/releases/latest`，不经服务端；匿名 60 次/小时/IP + ≥24h 节流；iOS 平台门不请求）。**下载在端内**（`update_downloader.dart`）：主链 GitHub asset CDN，兜底常量 `AppVersionInfo.selfHostedApkFallbackUrl`（VPS apk-sync 托管）；断点续传（.part+Range，206 追加/200 截断/416 完整）+ 每 URL 3 次退避重试 + 主链耗尽切兜底 + wakelock_plus 保活 + 完成大小校验。服务端 `/v1/app/version/latest` 保留但客户端已不再调用。apk-sync 保留（兜底托管）；**同版本号换包需先 `rm eatwise_server/downloads/.version` 再 up apk-sync**。CI release.yml = release 构建 + secret `KEYSTORE_BASE64`（本机 debug.keystore）签名，与本地产物同源；写入步骤有 sha256sum+keytool 指纹自检（基线：keystore `daa4ea6b…` / 证书 `79:94:78:99:…:B1:95`）。
- **生产安全基线（2026-09-19）**：公网仅 8443+22；`SMS_MOCK_ENABLED=false` 已上生产（mock 验证码 123456 链路关闭，501 SMS_CHANNEL_UNAVAILABLE，短信通道接入前手机号登录/注册不可用）；ADMIN_TOKEN 未配（x-admin-token 兜底关闭）；fail2ban 守 sshd。
- **用户偏好跨端同步（D-21，2026-09-18 入库）**：用户级偏好（语言/主题/体重单位/运动目标）随账号同步，设备级状态（AI 配置/密钥、模型下载、健康授权、通知权限、账号标识缓存）留本机不进包。服务端 User 增 `settingsPrefs` JSON 列（PATCH /users/me 字段级 LWW 整包替换，PATCHABLE/userView 已贯穿双驱动）。客户端协调器 `features/settings/application/settings_prefs_sync.dart`：同步包键约定 `{locale, theme, weightUnit, burnGoalKcal, stepsGoal, syncedAt}`（新增键只追加，pull 缺键跳过/非法值忽略）；push=四个偏好 provider 变更经 ref.listen 整包上行（失败静默），pull=登录成功/启动恢复会话下行（远端 syncedAt 新于本地 lastPrefsSyncedAt 才应用，回环靠 applying 标志）。Provider 懒加载——pull 挂接点（main/login/register）首次读取后 push 监听才生效。
- **未入库食品乐观入账 + 审核联动（2026-09-19 入库）**：「先记录，审批不过再清除」。服务端 `sync.service.snapshotOf` 回落本人自定义食物（此前只查共享库 → 自定义食物记录上行必 4xx → 客户端 T7 回滚静默删除，即「不报错就没了」根因；`RecordSyncFailure` 流原本无监听）；`reviewFoodCandidate` reject 幂等（重复驳回 200 返回当前态，reason 不覆写）且 kind≠correction 时级联软删贡献者该食物的 FoodEntry（`softDeleteFoodEntriesByFood`，双驱动）。客户端：`_push` 守卫（食物行缺失/customSyncPending 保持 pending 不放行吃 4xx）+ syncNow 自定义食物先于记录上行；条目营养按**快照口径**（approve/纠错改值不回溯）；「审核中」徽标复用 Foods.contributionStatus（今日记录行展示）。状态感知不做推送：`ContributionReviewSync`（`features/record/custom_food/application/contribution_review.dart`）在记录同步/进入记录页时拉「我的贡献」，diff 纯逻辑 `domain/contribution_review_logic.dart`（首次见到静默建基线，只认 pending→终态迁移）；approved 去标记、rejected 清本地记录+聚合重算+一次性驳回通知（prefs 队列 `ContributionStatusStore`，记录页 drain 展示，i18n `record.customFood.reviewRejectedNotice`）。无表结构变更（drift 仍 v10，prisma schema 不变）。
- **用户角色体系 + 移动端审批中心（2026-09-19 入库）**：User 增 `role`（'user'/'admin'，默认 user；prisma migration 20260919120000 + 双驱动贯穿；userView 带 role 但 **role 不在 PATCHABLE**——本人不可自助提权）。管理台设角色：`PATCH /v1/admin/users/:id/role`（AdminAuthGuard + @AdminRole('admin')，reviewer 403；幂等），console 用户页签角色下拉（一般用户/管理员，非 admin 身份只读禁用）。移动端审批：`GET/POST /v1/moderation/food-candidates[/:id/review]` 走**用户 JWT**（全局 JwtAuthGuard）+ `UserAdminGuard`（`src/admin/user-admin.guard.ts`，role!=admin → 403 明确提示不 404；匿名 401），复用 FoodService 同一审核池/同一 reviewFoodCandidate（含驳回级联清理）；FoodCandidate 增 `reviewedBy` 可空列留痕（管理端=管理员账号 id、token 兜底 null；移动端=用户 id）。客户端：`UserMeView.role`（缺省按 user 门控关闭）经 userMeProvider 流转；设置页账号组 role==admin 才显示「审批中心」入口（`/moderation/food-candidates` → `features/moderation/`：pending 队列游标分页 + 详情展开（纠错建议值对照）+ 通过/驳回确认弹窗 + snackbar 反馈，i18n `moderation.*`）。
- **UI 走查瘦身 + 联动审计首批（2026-09-21, v1.13.2）**：首页删加号 FAB（底部导航已有记录入口）；数据页信号卡一句话建议收进落区徽标（`signal.zone.<nutrient>` InkWell → AlertDialog `signal.advice.<nutrient>`，四态规范「建议必读全」改在弹窗内保证）；专业数据表数值列 flex 2:5 + clip（禁「…」省略号）；方案标签人话化（i18n `fasting.home.planTag` 参数改 `fast` 小时数，「断食 16 小时 · 进食窗口…」）；iOS 更新完全依赖 App Store——关于组「检查更新」入口经 `updateCheckSupportedPlatformProvider` 隐藏（注入式门控，测试同 override 路径断言；UpdateChecker 的 iOS 平台门保留双保险）。联动审计 8 项缺口首批实施 4 项：①`AnonymousDataMigrator`（`features/record/data/`，登录态 syncNow 开头 prefs 标记幂等：drift 四表 anonymous→uid 换挂 + fasting localId 撞主键 uid 侧为准 + 聚合缓存清空靠回填重建 + Weight/Contribution/Streak 三 Store `migrateAnonymous`）；⑥streak 三 prefs 键 userId 命名空间 + 构造期旧全局键一次性迁移；②断食方案下行 `FastingPlanSync.pull()`（本地有生效方案短路，防覆盖匿名期未 flush 脏方案）挂 main/login/register 与 settingsPrefs 同点位；④extend() best-effort POST /fasting/extend（**增量语义** extendMinutes，服务端累计判 30/240 余量），失败入 `fasting_extend_pending_<uid>` 队列挂 flush 重放（clientRequestId 去重、payload 不改写保幂等、终态码丢弃）。服务端：purgeUserData 两驱动补删 moderation_queue（无 userId 列，按所属帖集合定位，prisma 须在匿名化前删）。**未实施留作后续**：③fastingRecord 进 /sync 链、⑤时区三口径（Asia/Shanghai 硬编码 / X-Timezone 实发 UTC / user.timezone 恒 null，结构性改动）、⑦settingsPrefs 键级 merge。
- **走查两连修（2026-09-21, v1.13.3）**：① 数据页专业数据表数值列**去单位**（单位收进表下注 `unitsNote`，i18n 双语）+ 全列 `FittedBox(scaleDown)`、名称列 flex 2→3，窄屏「2000 千卡」截「200…」「蛋白质」截「蛋…」根治（注意 widget 测试裸数字 finder 不再唯一，断言改带单位大字/计数/祖先 FittedBox）。② 「当前按默认目标估算」误提示根治：`nutritionGoalProvider`（mini_signal_cards.dart）不再只信本地快照——快照缺失**或 usedFallback=true**（身体档案表单 sex/活动水平可空，保存即落兜底快照）时按「本地 `loadProfile()` ∪ 服务端下行档案」重算：`profile_sync.dart` 新增 `serverProfileOf`（userView 逆解析）/`mergeProfileWithServer`（本地非空优先 `??` 回落 remote；进食障碍筛查敏感仅本地）/`activityLevelOf`；`userMeProvider.valueOrNull` 同步读缓存（未登录/U1 未加载＝null＝旧兜底行为）；重算仍 `!isComplete` 才保留 usedFallback（有旧兜底快照沿用原值防口径抖动）。纯读侧重算不落盘，权威快照仍由 BodyProfileService 保存档案时写。
- **喝水提醒 + 走查两连修（2026-09-21, v1.13.4）**：① 喝水提醒：非断食期间（=进食窗口内）每本地整点一条，触发点/建议量全部由生效方案窗口派生——`features/record/domain/water_reminder_plan.dart` 纯函数（48h 视界、按归属日分组、达标日整日剔除、建议量=当日剩余量÷剩余触发点数上取整对齐 50ml 档递减分配；**跨午夜窗口次日晨段必须借相邻日网格 k∈[-24,48) 生成，只扫当日 0..23 网格会漏**，单测钉死）。装配在 `water_reminder_planner.dart`（prefs `water.reminder.enabled` 默认开、渠道 `water_reminders`、i18n `notification.water.hourly`）；`rescheduleWaterReminders(WidgetRef)` 挂喝水入账/撤销/设置开关（reschedule 是异步的，异常要 async 闭包内 try，同步 try 拦不住 `cancelAll` 的 MissingPluginException——曾打挂 widget 测试）。**⚠️ cancelAll 互删坑（本条核心）**：`LocalNotificationService.cancelAll` 是插件全局取消，全 App 唯一「cancelAll 再重建」入口 = `FastingNotificationScheduler.reschedule`——喝水经 `extraPlanner` 回调在同一循环排程（生成失败 try 静默），任何新功能要加通知**必须走这个口，别自己 cancelAll/schedule 独立调度器**。NO_PLAN 语义：断食侧清空但 extraPlanner 仍执行，喝水侧按 D-03 口径**回落 16:8 进食窗口**（跳过引导用户全程非断食，是提醒目标人群，不整日静默）。id 空间 `(sec~/60)*10+kind`：断食 kind 0/1/2、喝水 kind 3（`waterReminderNotificationId`），同分钟不撞。触发时刻带上 `extensionMinutes`（否则延长中断食会把锚点挪回旧时刻）。设置开关开启时 `requestPermission()`（全仓此前无人调它，「用时申请」合规 §3 时机即挂这）。② 走查修：语音/键盘未命中分桶——`transcript.typed` 时文案 `record.voice.noMatchTyped`（「没找到匹配的食物…」）+ 原文预填回搜索框（`recordSearchPrefillProvider`），语音继续用原 `voiceNoMatch`；**添加食物稳定出路**：搜索结果尾部固定「添加「<query>」为自定义食物」行（query 非空即出现——模糊匹配总有不相干命中，只靠空态 CTA 用户永远看不到添加入口），`startCustomFoodFlow`/`CustomFoodSheet` 增 `initialName` 预填（注意空表单校验测试需先清空预填）。
- **自定义食物改/删/详情页分享审核（2026-09-21, v1.13.6）**：服务端 `PATCH/DELETE /v1/foods/custom/:id`（`UpdateCustomFoodDto` 同创建去 clientRequestId——更新 LWW **不做幂等**，重放同值无害；owner+isCustom 否则 404）。删除=**软删 Food 行**（schema 加 `deletedAt` + migration 20260921120000——FoodEntry FK 无 onDelete，prisma 硬删 FK 炸；读路径全量 `deletedAt:null` 过滤：findFoodById/findFoodByBarcode/findCustomFoodById/findCustomFoodsByUser/searchFoods/promoteCustomFoodToShared，漏一处=「删了扫码还能记」幽灵）+ `softDeleteFoodEntriesByFood` 级联（tombstone 走 sync/pull 下行清他端）→ `{deleted,deletedEntries}`；pending 候选 409 `FOOD_UNDER_REVIEW`（审核结论要回写该行），rejected 可删（candidateView 容错 food=null）。**contribute rejected 重提交**：原实现对已有候选一律幂等返回原状态→rejected 永不回池（UI 反馈失真死路）；现 rejected 重提交=同一候选行原位重置 pending（`updateFoodCandidateStatus(id,'pending','',null)`——清空 reason 必须传 `''` 不是 null，签名 reason 的 null=不动）。客户端 `CustomFoodRepository.update/delete`：update 本地先行+网络错静默，**pending 行（离线新建未上行）例外不发 PATCH（服务端无行必 404）且轮换 customClientRequestId**——否则 retryPending 旧键带新内容必吃 PAYLOAD_MISMATCH 永久卡 pending；delete 远端先行（409/网络错本地不动可重试），成功后逐条 `deleteEntry` 两态级联（内部 `_recompute` 已按归属日重算，跨日天然覆盖）+ `foodDao.deleteById`。入口=FoodDetailSheet isCustom 动作行（编辑=CustomFoodSheet `editTarget` 编辑态回填/隐藏分享勾选、分享=pending 隐藏按钮重读行刷徽标、删除=确认含记录条数→snackbar→invalidate+syncNow）；rejected 重提交后徽标靠 `_onShare` 重读本地行刷 pending。i18n `record.customFood` 复数键 zh 必须补 `one`（record_i18n_test 按叶子 key 对集合，en one+other/zh 只 other 必红）。
- **三合一发版（2026-09-22, v1.13.9）**：① 食物库扩充：`eatwise_data/scripts/import_cfct.py`（CFCT 中国食物成分表 CSV→cfct_foods.json 1644 条）+ `gen_dishes_snacks.py`（中式熟食/零食/饮料 curated 185 条，kcal=Atwater 4P+4C+9F 反算〔待营养背书〕；撞 id 以旧文件为准）；`build_seed.py` SEED_VERSION=2026.09.1，curated_entries() glob 全 curated 目录；**seed 总量 9284**（usda 7283+cfct 1644+curated 357），VPS migrate 服务自动 upsert 补灌。② 安全批：SMS_MOCK_ENABLED 白名单 `==='true'` + production bootstrap fail-fast；JWT 生产拒占位值/长度<32（auth.module resolveJwtSecret）；全局限流 ThrottlerGuard APP_GUARD 绑定 + `THROTTLE_LIMIT` env（默认 300/min）+ main.ts `trust proxy 1`（**没有 trust proxy 时全站共享 Caddy IP 一个桶**，线上 429 误伤先查这个）；evidenceImageUrl `@Matches` 协议白名单（相对路径 /v1/uploads 合法所以不能用 @IsUrl）；batchGet DTO 上限 200；X-Timezone isValidTimezone 校验回退（nutrition/streak 同 fasting 口径）。③ 逻辑批（走查 L1-L6）：retryPending 扩扫 submitting+conflicted（`pendingEntries(userId, {statuses})`——**textEnum 列按枚举索引落库，多值匹配必须 `equalsValue` OR，`isIn(names)` 存储口径错位永不命中**；跳过依据用 `_pushTimers` 不是 `_undoDeadlines`——后者离线入账也登记）；conflicted 行落 serverVersion 以新基线 LWW 收敛；exercise/water create 被 VALIDATION_ERROR 永久拒绝→本地清出（保 pending 每轮重复上行永不归零）；fasting 脏方案 flush `_flushing` 互斥 + PUT 后 compare-and-delete（防旧 PUT 覆盖新方案+误擦脏标记）；**纠错驳回不清记录**（对齐服务端 food.service 口径；通知队列 RejectedNotice{name,correction}，旧纯 String 格式兼容读）；settingsPrefs syncedAt **服务端 patchMe 统一打戳**（客户端存回包值——设备墙钟超前者不再永久压制他端，LWW 时钟权威在服务端）。④ UI：我的贡献页 + 入口；记录页确认卡 ConstrainedBox(maxHeight 90%) + 内滚（旧实现内容高时按钮被顶出屏幕；结果列表保持 Expanded 常驻——曾试过 `if (selected==null)` 隐藏列表，直接破坏「保存后搜索见审核中标签」既有语义）；感知设计四处（落区徽标 info 图标着色/纠错入口 TextButton.icon/审批卡 chevron/展开文本水波纹）。
- **数据治理 + 记录页搜索性能（2026-09-22, v1.13.10）**：① seed 收敛：`build_seed.py` 同名对账三分法（值近似→curated 并入 cfct 别名后出 seed / 形态差→cfct 行加状态限定词两行并存 / cfct 内部品种粒度保留；禁止生干值覆写熟态值）+ `removedIds` **累计**清单进 seed 顶层（幂等可重放；同版本重跑也必须并入 prev 累计，否则链条断裂）；App `FoodSeedLoader.ensureSeeded` 调 `FoodDao.deleteBuiltInByIds`（只删 isCustom=false 且未被 food_entries 引用行），prisma seed 末尾 `updateMany` **软删** deletedAt（FoodEntry FK 无 onDelete 硬删违约；内存驱动冷启动重建天然无残差）。② 双语硬约束（用户拍板）：校验规则 E4 = name_en/aliases_en 禁中文；CFCT 源 433 行英文名缺失由 `import_cfct.py` `EN_BASE_NAMES`（202 基名人工审校译文）补齐；**USDA SR Legacy 7283 行（无中文名）默认管线裁剪**（`--include-usda` 保留通路），全库收敛 1958 条 100% 双语（cfct 1644+curated 314），removedIds=7326。CFCT 源仓库（Sanotsu/china-food-composition-data，纸书 OCR 衍生）**无开源许可证**，README 如实标注端内使用口径。③ 记录页搜索重构（stale-while-revalidate，根治「一直转圈」）：`recordFoodSearchProvider` 改 StreamProvider **常驻流**——`ref.listen` 而非 watch（riverpod 依赖变更会让 AsyncValue 回退 AsyncLoading，listen 规避）、本地命中立即推、远端 200ms 防抖后追加、generation 计数丢过期响应、空词零网络请求；消费侧 `valueOrNull` 保留旧结果在屏，`.when` loading 分支删除。实测 1958 行四列 LIKE 全表 <7ms——**FTS5 方案实测否决**（trigram 分词中文 2 字词不命中，中文搜索主力恰是 2 字词），无索引无迁移。record_page_test 时序契约（enterText 后 ≤100ms 出结果）由本地先行路径天然满足；`ref.invalidate(recordFoodSearchProvider)` 兼容常驻流（重建后 fireImmediately 重查）。
- **数据治理修订 + 分块修复（2026-09-22, v1.13.11）**：v1.13.10 三处后续修。① **同名吸收方向翻转**（FK 暴露最小化）：值近似组保留 **curated id**（月龄大、有存量 FoodEntry 引用），数值覆写为 CFCT 权威值（source 改标 cfct、别名互并、note 记吸收来源）；出 seed 的是 cfct 孪生行 id（一日龄 ≈零引用），prune 零 FK 暴露——v1.13.10 反方向会把月龄 curated id 送进 removedIds（正是 v1.13.6 FK 教训的暴露面）。配套：`removed_ids = (prev | stale | dropped) - new_ids`（本版复活 id 摘出清单，prune 不得删活行）；prisma seed upsert `update` 带 **`deletedAt: null` 复位**（历史软删 id 复活防「导入即死」；App 端天然安全：upsert 在前 prune 在后 + 交集恒空）。seed 2026.09.23：1956 条（cfct 1616+curated 340），removedIds 7328。② `FoodDao.deleteBuiltInByIds` **500 分块删除**——removedIds 7000+ 单条 `isIn` 在 Android 11- SQLite 变量上限 999 直接 `too many SQL variables` 崩导入（真机必炸，dev 内存库测不出）。③ 限定词证据纪律：状态限定词只取该行 `name_en` 文本证据（dried/raw/flour/semisoft/roasted/whole），营养推断入 `note` 存疑不改名（虾仁 199kcal 撤「(干/制品)」；豆腐干「油炸」改「半干」）；W2 规则（出 seed 同名残留 warning）进 validate()，CI data job 跟踪，防下批数据静默回归米粉/河粉级双行事故。
- **时区三口径修复 + 审核徽标自愈（2026-09-22, v1.13.12）**：① **tz.local 恒 UTC 根因**（真机走查「记录全默认早餐」+ X-Timezone 实锤 `Etc/UTC`）：timezone 包 `initializeDatabase` 末尾显式 `setLocalLocation(UTC)`，旧 main 只装载不设置——餐次预判/归属日/X-Timezone 三口径全按 UTC 小时错位。新增 `core/time/timezone_bootstrap.dart`：`bootstrapTimezone(tzfBytes)` init 后按 **offset 匹配** setLocalLocation（flutter_timezone 新增原生 pod 不划算已撤；同偏移优先 Asia/Shanghai，失败必回落 UTC——`_local` 是 late 不设则后续读 LateInitializationError）。消费口统一 `deviceLocationFallback()`：`_bootstrapDone` 门控——生产返回 tz.local，**测试进程不跑 main 恒 false → 返回 Asia/Shanghai 与历史硬编码口径一致**（曾直接改 `deviceLocationProvider => tz.local` 打红 17 个测试）。② 下行**占位食物行**：`RemoteRecordSync._applyChange` 食物行缺失但 change 带快照时不再 skip（seed 缩量 removedIds 行永久消失，skip 等不来未来、游标永久卡页），`_ensurePlaceholderFood` 快照总量÷grams×100 换算每 100g upsert 幂等落库（名称=foodId 字面量，数据非文案不 i18n）；skip 语义收窄=缺 foodId 或缺快照（**无条件检查**，勿嵌进缺食物行分支——测试钉死）。③ 审核徽标不自愈（「已审批仍显示审核中」）= 两客户端缺陷叠加：服务端重启窗口 Caddy 502 被引擎静默吞（`RecordSyncEngine` 五处子同步 `on ApiException` 放宽为 `on Object`+debugPrint）+ **`entryFoodProvider` 一次性 FutureProvider 无失效**。修=`ContributionReviewSync` 增 `onStatusApplied(foodId)` 回调（custom_food_providers 装配处 `ref.invalidate(entryFoodProvider)` 整族失效）；`ModerationController.review` 成功后即时回写本地行 `_applyLocalFoodStatus`（只动 pending 行，同设备自批不等下轮）；存量校正升级为**按 foodId 取 updatedAt 最新终态为权威**（同食物多贡献迭代顺序漂移）且**状态不等即改写**（覆盖 rejected→approved 重提交残留；旧实现只覆写 pending 行）。④ 拍照识别流补 `MealTypeChips`（整批共用一个餐次，initState 复位 provider）。

- **五项走查批量修复（2026-09-22, v1.13.13）**：① **管理台「食物库」页签**（删重复食物）：`GET/DELETE /v1/admin/foods`（新 `admin-foods.controller.ts`，搜索 reviewer 可用、删除 `@AdminRole('admin')`），StoreDriver 新原语 `softDeleteFoodById`（去 isCustom 条件的软删，任意来源行）+ `softDeleteAllFoodEntriesByFood`（**跨用户**级联 tombstone，prisma 显式 updatedAt 保 sync/pull 游标）；pending 候选关联 409、缺行 404；console 新 tabFoods（搜索/删除 confirm/级联条数回显）。**级联语义注意**：删共享行会软删所有用户引用条目（快照口径不回溯，但记录消失）——删前先核 `SELECT count(*) FROM food_entries WHERE foodId=... AND deletedAt IS NULL`。② **iOS 重装三连**（`flutter install` = 卸载重装）：Keychain 令牌存活但 SharedPreferences 被清 → restore 成功却重弹引导（v1.12.4 双写只挂登录/注册响应，恢复路径无触发点）。修复=restore 后本地标记未完成时 fire-and-forget `GET /users/me` 回填（`AuthApi.fetchOnboardingStatus`，异常静默、本地已完成则零请求）；`OnboardingGate` 改 ChangeNotifier 且并入 router `refreshListenable`（异步翻转要能把已停在 /onboarding 的用户带走）。**若令牌也被清（真未登录）**：设置页账号组按 `authGate.loggedIn` 门控——未登录隐藏 登出/删除账号/修改密码，显示「登录」tile；已登录但标识取不到显示「点击重试」（invalidate userMe），「未登录」占位只在真未登录出现。③ 记录页聚合行折行：`record.page.todayKcal` 缩短 + 「待校准」拆独立描边徽标（`calibrationBadge`）。④ 饮水/体重卡片：`IntrinsicHeight + CrossAxisAlignment.stretch` 拉平、体重值行 textSm 统一 + `record.weight.tapToEdit` 灰色提示行。⑤ 首页「完成今天断食，开启第 1 天」**非 bug**（取证：该用户 4 条 fasting_records 全部 broken 未达标 → streak=0 权威口径正确，banner 空态引导文案与方案窗口正交）。数据侧：生产重复食物「蔬菜沙拉」community 行（protein 15 存疑、零引用）已手工软删，保留 curated 行。

- **走查五连修（2026-09-22, v1.13.14）**：① 断食「没中断却全 broken」归因：历史记录=旧版 App X-Timezone 误报 UTC 口径期产物（v1.13.12 已修 header，判定本身无错）；本批加固=服务端 `fasting.service.getStatus` 对**过期 on_track ghost** 自动结算（私有 `autoCloseExpired`：result=completed/达标按终点/`auto_closed` 事件留痕/streak 重算——无 F2 中断上报=无中断证据，按计划终点自然结束〔拍板：否掉 broken 保守口径〕；eating 分支回显链补 `findFastingRecordByPlannedEnd`，防 ghost 劫持物化）。② 「服务端删了食物客户端还显示两条」：定性=用户贡献转正行非 seed 行；新增**行级反查幽灵收敛**（`findGhostContributionFoodIds`：本地 contributionStatus 非空但服务端 foods 集合查无此行→自定义行删记录+重算聚合+删行+`removed` 通知 / 共享行只清徽标；known 表格式不动、pending 起点也清——候选整行消失=已删无歧义窗口；首轮全量分页安全）。③ 设置页「点击重试」永挂：`userMeProvider` 吞错 null 被当成功值永久缓存——失败时挂 30s `Timer→invalidateSelf` 自愈。**终态形状（v1.13.15）**：timer 为 body 局部变量、`ref.onDispose(() { disposed=true; timer?.cancel(); })` 注册在**首个 await 之前**、catch 里 `if (!disposed) timer = Timer(...)`。⚠️ 三件套缺一不可：riverpod 2.6.1 element 未挂载时 `ref.onDispose` 直接抛 StateError（catch 里才注册=dispose 后落地必抛+timer 无人取消）；unmount 确实 dispose container，**disposed 守卫**拦「container 先 dispose、future 后落地」新建的 timer（否则 fake_async 永挂 pending timer，scratch A/B 实证 onDispose 时机救不了）；invalidateSelf 重跑 body 前 runOnDispose 已取消上一轮 timer，模块级标志多余已删。④ 「待校准」历史徽标删除（本地聚合即权威口径）。⑤ **审批中心删除功能**：服务端 `DELETE /v1/moderation/food-candidates/:id`（用户 JWT+UserAdminGuard）与 `DELETE /v1/admin/food-candidates/:id`（管理台同款）→ `FoodService.deleteFoodCandidate`：缺候选 404 不幂等；kind≠correction 时同 foodId 其它 **pending** 候选→409 `FOOD_UNDER_REVIEW`；approved=跨用户级联下架（`softDeleteAllFoodEntriesByFood`），pending/rejected=贡献者作用域；correction 只删审核痕迹；StoreDriver 新原语 `deleteFoodCandidateById`。客户端审核页删除按钮→确认弹窗→**同设备直清**（`ModerationController._clearLocalFoodOnDelete`：custom/barcode 删记录+重算+删行，correction 撤 pending 徽标；不等下轮同步），跨设备贡献者靠②行级反查兜底。**v1.13.15 收尾加固**：删除 409 守卫弃用 `findFoodCandidateByFoodId`（findFirst createdAt 升序会先命中被删行本身、漏检后来的 pending 孪生→approve 已软删行），改扫 `listFoodCandidates('pending')` 全池按 foodId 匹配（回归用例钉死）；本机直清与同步轮 ghost 清理均补 `ref.invalidate(recordFoodSearchProvider)`（搜索常驻流不自刷已删行）。

## 后端（eatwise_server/）

```bash
npm run start:dev    # 默认内存 DataStore（STORE_DRIVER=memory）
npm test             # 单测；RUN_PG_TESTS=1 + DATABASE_URL 时含 pg 集成
npm run test:e2e
```

- 真实库：`docker compose up -d postgres && npx prisma migrate deploy`，`STORE_DRIVER=prisma` 启动。
- 云主机自部署：`Dockerfile` + `docker-compose.prod.yml` + `deploy/`（Caddyfile/env 模板）已入库，完整步骤见 `docs/tech/部署-云主机自部署-v1.0.md`。
- **VPS 代码同步坑（2026-09-21 实炸）**：VPS 无 git，更新代码靠 rsync——**永远用目录级目标**（`rsync -az src/ host:.../src/`、`rsync -az prisma/migrations/ host:.../prisma/migrations/`）。多源文件写法（`rsync prisma/schema.prisma prisma/seed.ts dest/`）会把路径拍平落到 `dest/schema.prisma`，杂散根级 schema 经 `COPY . .` 进镜像后劫持 prisma CLI 默认解析（日志特征：`schema loaded from schema.prisma` + `No migration found in prisma/migrations`）→ migrate deploy 空转、新列缺失、seed P2022。判据只认 migrate 日志的 applied 行 + `\d foods` 列。
- 持久化（2026-09-08 收口完成）：业务 Service 全部走 StoreDriver 接口，`STORE_DRIVER=prisma` 即真实落库（含用户/令牌/断食/饮食/饮水/体重/streak/帖子/点赞举报/审核队列/自定义食物/幂等键）；仅 smsCodes（mock）与管理端登录限流保留内存。prisma 模式启动前需 `npm run prisma:seed` 灌食物库（内存模式由 main.ts 自动灌）。
- 体重记录（阶段 C，2026-09-17）：`weight_logs` 表 + `/v1/weight-logs`（POST 幂等 upsert 同 userId+date 覆写 / GET ?from&to / DELETE 软删）；客户端 WeightLogStore 按用户命名空间（v2 key，v1 全局键一次性迁移），两态 pending/synced 经 RecordSyncEngine 推拉（仅登录态）。
- 图片存储（2026-09-14 迁移完成）：`STORAGE_DRIVER=local`（默认，落 uploads/，仅单实例）/ `s3`（R2/S3 + CDN，需 `S3_BUCKET`/`S3_ACCESS_KEY`/`S3_SECRET`/`CDN_BASE_URL`，R2 另需 `S3_ENDPOINT`，缺失启动即报错）；s3 模式 GET /v1/uploads/:filename 302 到 CDN，端点契约不变。
- 管理控制台：`/admin` 静态页（`src/admin/console/index.html`，原生 JS 单文件，nest-cli assets 拷到 dist），数据走 `/v1/admin/*`。鉴权为管理员账号体系（`POST /v1/admin/auth/login` 发 12h JWT，角色 admin/reviewer，见 `src/admin/admin-auth.*`）；env `ADMIN_USERNAME`/`ADMIN_PASSWORD` 齐备时启动自动种 admin 账号，`x-admin-token` 为过渡兜底（视为 admin）。

## Git 与 CI

- **不要提交**：`eatwise_data/raw/`（213MB 原始数据，gitignored）、`.tooling/`、`node_modules/`、`dist/`。
- CI 6 job：app（format/analyze/test）、build-android、build-ios、server、server-pg（真实 pg）、data。全绿才可合入。
- 发版：`git tag v*` 推送 → 自动构建 APK + GitHub Release。

## 已知外部依赖（不要在代码里硬编）

- 推送/识别/内容审核均为 stub 或抽象层，凭据与选型见 README「待外部确认」。
- 营养公式与阈值代码里有，但属「待营养背书」状态，数值改动必须同步 `docs/specs/规格-营养规则-TDEE公式与信号灯阈值-v1.0.md`。
