# 《轻断食与健康饮食 App 产品需求文档(PRD)》

> 本 PRD 由《轻断食与健康饮食 App 产品设计方案定稿》反推 / 补齐而成。
> 设计稿解决「长什么样」,本文档解决「做什么、为什么做、做到什么程度、如何验收」。
> **阅读约定**:标注 `〔假设〕` 的为基于设计稿合理推断、尚需团队确认的内容;`〔待外部确认〕` 为需营养专业侧 / 法务书面背书的项(开发可并行,上线前必须关闭)。v1.0 中的开放问题已全部拍板,决策编号 D-01～D-20,逐条见《明食 · EatWise — 开放问题决策记录》v1.0(下称「决策记录」),本文引用处均标注编号。

---

## 文档信息

| 项 | 内容 |
|----|------|
| 文档名称 | 轻断食与健康饮食 App 产品需求文档 |
| 产品名称 | 明食 · EatWise |
| 版本 | v1.1(评审回填版) |
| 状态 | 已评审·待开发 |
| 关联文档 | 《轻断食与健康饮食 App 产品设计方案定稿》(设计规范 / Design Spec);《明食 · EatWise — 开放问题决策记录》v1.0(决策锚点,D-01～D-20);《规格-M2 断食计时状态机》《规格-M5 streak 与补签卡》《规格-数据同步与四态持久化》《规格-i18n 中英双语》《合规-隐私与合规方案》(均为决策记录指明的下挂规格文档) |
| 适用范围 | iOS / Android(Flutter 单代码库双端,见 D-17),Web 后台为后续阶段 |
| 撰写日期 | 2026-06-25(v1.1 修订于 2026-07-27) |

### 版本记录

| 版本 | 日期 | 修订人 | 说明 |
|------|------|--------|------|
| v1.0 | 2026-06-25 | — | 基于设计定稿首次形成需求侧 PRD |
| v1.1 | 2026-07-27 | — | 回填需求评审结论与《开放问题决策记录》(D-01～D-20):问卷/推荐/TDEE(M1)、计时归属与容差(M2)、撤销窗/食物库/四态同步(M3)、红黄绿阈值(M4)、streak 口径与补签卡(M5)、登录与同步(M7)、中英双语国际化、数据模型字段补全、开放问题全部关闭;状态由「待评审」变更为「已评审·待开发」 |

---

## 一、项目背景与目标

### 1.1 背景

轻断食(间歇性断食,如 16:8)正成为主流的健康管理方式,但现有工具普遍存在三类问题:**记录繁琐难坚持、营养反馈过于专业看不懂、缺乏即时正反馈与社交激励导致中期流失**。目标人群有明确的减脂塑形 / 改善作息 / 改善体检指标的动机,但缺少一个「门槛低、看得懂、能坚持」的陪伴型工具。

### 1.2 产品定位

一位「懂你节奏的轻断食陪伴者」——把专业的断食与营养知识,翻译成人人能懂、随手能做的健康习惯。

### 1.3 业务目标

| 层级 | 目标 |
|------|------|
| 北极星指标(D-01) | **周活跃且完成 ≥4 个「有效记录日」的用户数(WAU-Active)** —— 直接反映「坚持」。有效记录日 = 当日断食打卡达标(D-08)**或**当日有 ≥1 条饮食记录 |
| 一级目标 | 提升新手转化(完成引导→启动方案)、提升记录留存(对抗记录期流失) |
| 二级目标 | 提升中期留存(对抗 2–3 周动力衰减)、形成社交增长 |

### 1.4 成功度量(Success Metrics)(口径已决策,见 D-01)

| 指标 | 定义 | 目标值(MVP 上线 3 个月) |
|------|------|------------------------|
| 引导完成率 | 完成 3 题问卷并启动方案 / 新用户 | ≥ 70% |
| 次日记录留存 | 注册次日仍有记录行为的用户占比 | ≥ 45% |
| 7 日断食打卡留存 | 注册 7 日内仍在使用断食计时的用户占比 | ≥ 30% |
| 单次记录耗时 | 一次饮食记录从进入到确认的中位耗时 | ≤ 15 秒 |
| 中期留存(D21) | 第 21 日仍活跃用户占比 | ≥ 18% |

> 以上五项目标值已按 D-01 确认为验收口径,纳入埋点规范统一埋点。

---

## 二、目标用户与场景

### 2.1 用户画像(承接设计稿,作为需求依据)

| 画像 | 核心动机 | 关键痛点 | 对应产品诉求 |
|------|----------|----------|--------------|
| **林悦** · 28 岁 互联网运营 | 减脂塑形、改善作息,认可 16:8 | 进食时间不规律;记录繁琐;不懂热量/营养换算 | 极速记录、断食提醒、营养"翻译" |
| **张涛** · 24 岁 应届程序员 | 体检指标偏高,需"被指导"入门 | 信息过载;怕断食伤身;缺外部激励 | 新手引导、原理科普、方案一键启动 |
| **陈梦** · 32 岁 市场经理 | 靠社群氛围与成就感维持习惯 | 单人枯燥;正反馈不足;记录与社交割裂 | 打卡成就、挑战赛、好友互动 |

### 2.2 核心用户旅程与流失点

```
认知 → 启动方案 → 日常记录 → 数据反馈 → 习惯维持
  │        │          │(最高频流失)   │           │(中期流失)
门槛高   缺个性化引导   步骤多/耗时    看不懂/无建议   缺即时反馈与激励
```

本 PRD 的功能设计围绕「在每个流失点放一个对应功能」展开(见第六章映射表)。

---

## 三、范围与版本规划

### 3.1 版本规划(对齐设计稿 P0/P1/P2)

| 版本 | 目标 | 包含模块 |
|------|------|----------|
| **V1.0 / MVP(P0,决定留存)** | 跑通「看状态—快记录—被引导」核心闭环 | M1 新手引导与方案库、M2 断食计时与提醒、M3 快捷饮食记录、M7 账号与数据基础、M8 无障碍基线 |
| **V1.1(P1,提升粘性)** | 让用户「看得懂、被激励」 | M4 营养可视化分析、M5 社区打卡(基础) |
| **V1.2(P2,增长差异化)** | 社交与深度 | M5 挑战赛/好友、M6 深度营养报告与趋势 |

### 3.2 明确不做(Out of Scope,MVP)

- 不做硬件 / 可穿戴设备数据接入(MVP 与 V1.1 均不做,V1.2 后根据用户反馈再评估,见 D-19)。
- 不做医疗诊断、不输出医疗建议(仅健康习惯陪伴,需在引导页声明免责,免责声明中英双语,见 D-18)。
- 不做食谱电商 / 商城。
- 不做 Web 端用户产品(后台除外)。
- 不做营养师人工咨询。
- 不做 5:2 断食法的完整计时流(仅作说明引导,列为 V1.1 候选,见 D-03)。
- 不做 Google 登录(随英文版海外上架评估,V1.1+,见 D-13)。

---

## 四、功能需求(核心)

> 每个模块统一采用:**概述 → 功能点 → 业务逻辑/规则 → 用户故事与验收标准 → 异常与边界 → 数据与埋点**。

---

### M1 新手引导与方案库(P0)

**概述**:通过极简问卷为新手生成个性化断食 + 饮食方案并一键启动,降低"不知选哪种断食法"的门槛。

**功能点**
1. 3 题问卷引导页(目标、当前作息、经验水平,题目与选项已定,见 D-02)。
2. 方案库:内置多种断食法(16:8 / 14:10 / 18:6 / 5:2 等)及说明(5:2 仅说明引导,不提供完整计时流,D-03)。
3. 个性化推荐:根据问卷结果推荐 1 个主方案 + 备选(规则表见 D-03)。
4. 一键启动:确认后写入用户方案,初始化断食窗口与每日营养目标。
5. 原理科普卡:针对"怕伤身"提供断食原理可视化(承接设计稿信息图)。

**业务逻辑/规则**

- **问卷题目(D-02)**:3 题,单选,可跳过(跳过走 D-03 兜底):

| 题号 | 维度 | 选项 |
|------|------|------|
| Q1 | 目标 | 减脂塑形 / 改善体检指标 / 调整作息 / 先试试看 |
| Q2 | 作息 | 规律(朝九晚五)/ 轮班或夜班 / 自由职业·不规律 |
| Q3 | 经验 | 零基础 / 试过但没坚持 / 有断食经验 |

- **方案推荐规则表(D-03)**:

| Q3 经验 | 主推荐 | 备选 | 默认进食窗口 |
|---------|--------|------|--------------|
| 零基础 | 14:10 | 16:8 | 10:00–20:00 |
| 试过没坚持 | 16:8 | 14:10 | 12:00–20:00 |
| 有经验 | 16:8 | 18:6 | 12:00–20:00 |

  - 附加规则:Q1=改善体检指标 且 Q3=有经验 → 主推荐升级为 18:6,备选 5:2(5:2 仅作说明引导)。
  - Q2=轮班/不规律 → 推荐理由中提示「窗口可自由调整」,不设特殊方案。
  - 跳过问卷 → 默认 16:8(12:00–20:00)兜底,不阻断进首页。
  - 推荐理由文案规则模板化(中英双语),随方案卡展示。

- **每日营养目标计算(D-04,公式待营养专业侧书面背书,开发可并行)**:
  - **BMR**:Mifflin-St Jeor 公式。
    - 男:`10×体重kg + 6.25×身高cm − 5×年龄 + 5`
    - 女:`10×体重kg + 6.25×身高cm − 5×年龄 − 161`
  - **TDEE** = BMR × 活动系数:久坐 1.2 / 轻度 1.375 / 中度 1.55 / 高度 1.725。
  - **目标热量**:减脂 = TDEE × 0.8;下限保护:女 ≥1200 kcal、男 ≥1500 kcal;维持/作息目标 = TDEE × 1.0。
  - **三大营养素配比**(供能占比):蛋白 25% / 碳水 45% / 脂肪 30%;换算系数 蛋白 4 kcal/g、碳水 4 kcal/g、脂肪 9 kcal/g。
  - **兜底默认值**(缺基础信息时):女 1800 kcal / 男 2200 kcal,按同配比折算,并引导补全资料。
  - 实现要求:规则引擎封装为**纯函数 + 全量单测 + 公式热配置**;M4 上线前取得营养专业侧书面背书(评审项 3)。
- 方案可随时在「我的」中更换;更换后**次日 0 点(本地时区)生效**,当日已记录数据保留,确认弹窗明示生效时间(D-06)。

**用户故事与验收标准**

> US-1.1 作为新手,我希望回答几个问题就拿到适合我的方案,这样我不用自己研究。
- 给定 新用户首次进入,当 完成 3 题问卷,则 按 D-03 规则表展示带推荐理由的主方案 + ≥1 个备选。
- 给定 已展示推荐,当 点击「一键启动」,则 写入用户方案、初始化进食窗口与每日营养目标(按 D-04 计算),并跳转首页显示断食状态环。
- 给定 用户跳过问卷,则 提供默认 16:8(12:00–20:00)方案兜底,不阻断进入首页。
- 给定 用户在「我的」中更换方案,则 弹窗明示「次日 0 点生效」,次日新窗口生效且当日数据保留。

**异常与边界**
- 用户缺失基础信息(身高体重等)→ 营养目标按 D-04 兜底默认值(女 1800 / 男 2200 kcal)兜底,并提示去补全。
- 问卷中途退出 → 进度本地保存,下次进入续答〔假设〕。

**埋点**:引导曝光、每题完成、方案推荐曝光、一键启动、跳过引导。

---

### M2 断食计时与提醒(P0,核心)

**概述**:可视化的断食/进食窗口计时,自动提醒进入与结束窗口,首页主视觉。这是留存的核心功能。

**功能点**
1. 首页断食状态环:倒计时数字 + 状态文案(断食中 / 进食中)+ 归属文案「本次断食计入 X 月 X 日」。
2. 主操作:「结束断食」「延长」双按钮。
3. 窗口提醒:进入断食窗口、结束断食窗口本地通知。
4. 锁屏小组件:实时倒计时同步。
5. 进食/断食窗口设置:用户可调整进食窗口时间段。

**业务逻辑/规则(关键,已按 D-07～D-10 拍板;状态机迁移表详见《规格-M2 断食计时状态机》)**
- **窗口定义**:用户设定进食窗口(如 12:00–20:00),其余时间为断食窗口。系统据此实时判断当前状态。
- **状态机**:
  - `断食中` → 倒计时到进食窗口开始 → `进食中`
  - `进食中` → 倒计时到进食窗口结束 → `断食中`
  - 支持手动「结束断食」(提前进入进食)与「延长断食」(推迟进食窗口)。
  - 完整状态迁移表(含午夜破窗、跨时区、回拨系统时间等边界用例)以《规格-M2 断食计时状态机》为准,M0 第一周计时 Spike 验证后关闭(评审项 1)。
- **时间存储与跨天处理(D-07)**:全部时间戳 **UTC 存储**,渲染按设备本地时区换算;**打卡归属日 = 进食窗口所属自然日**(一次断食归入其后的进食窗口那天);计时环下叠加「本次断食计入 X 月 X 日」归属文案,锁屏小组件同步。
- **达标判定与破窗容差(D-08)**:
  - 完整走完断食窗口(进食窗口按时开启)→ 当日**达标**。
  - 手动「结束断食」提前 ≤15 分钟,或「延长」后最终断食时长 ≥ 计划时长 → 仍记**达标**。
  - 提前 >15 分钟破窗 → 当日**不达标**,记录实际断食时长,streak 按 D-12 处理。
  - 容差值 15 分钟走服务端配置,可热调。
- **延长规则(D-10)**:步进 30 分钟,单次最多延长 4 小时;延长后进食窗口相应后移,不压缩进食窗口时长。
- **提醒策略(D-09)**:进食窗口**开始前 15 分钟**提醒一次 + **到点**提醒一次;断食窗口开始(进食结束)到点提醒一次。使用**本地通知(local notification)**实现,不依赖推送通道在线;尊重系统勿扰与用户通知开关;提醒提前量与文案(中英双语)可在设置中调整,默认 15 分钟。

**用户故事与验收标准**

> US-2.1 作为用户,我希望随时一眼看到"现在该断食还是该吃",并被准时提醒。
- 给定 当前处于断食窗口,当 打开首页,则 状态环显示「断食中」+ 距进食窗口开始的倒计时 + 归属文案「本次断食计入 X 月 X 日」。
- 给定 到达进食窗口开始时间前 15 分钟,则 推送「即将可以进食」本地提醒;到点再推送「可以进食」提醒,状态环切换为「进食中」+ 暖阳橙弧。
- 给定 用户点击「结束断食」,当 当前为断食中且提前 ≤15 分钟,则 立即切换为进食态、记录实际断食时长并记当日达标;提前 >15 分钟则记不达标并明确告知。
- 给定 用户点击「延长」,则 进食窗口开始时间按 30 分钟步进后移,累计单次不超过 4 小时,且不压缩进食窗口时长。
- 给定 锁屏小组件已添加,则 其倒计时与 App 内一致(误差〔假设〕≤1 分钟),并同步显示归属日。

**异常与边界**
- 关闭通知权限 → App 内顶部横幅引导开启,不阻断计时(D-09)。
- 跨时区/手动改系统时间 → 以 UTC 存储 + 本地时区换算重算,边界用例按《规格-M2 断食计时状态机》验收(评审项 1)。
- 漏打卡(整天未触发)→ 连续天数中断逻辑见 M5(D-12)。

**双端差异**
- **iOS**:锁屏小组件用 WidgetKit 实现;iOS 16.1+ 增强为 Live Activity 实时刷新,低版本降级为普通 Widget(D-14)。本地通知用 UNUserNotificationCenter(经 flutter_local_notifications)。
- **Android**:小组件用 AppWidget(经 `home_widget` + 原生桥),无 Live Activity 等价能力,以 Widget 定时刷新实现;本地通知走 NotificationChannel(API 26+,D-14)。

**埋点**:首页状态环曝光、结束断食、延长、窗口设置修改、提醒送达/点击、小组件添加。

---

### M3 快捷饮食记录(P0,最高频流失点)

**概述**:用最低摩擦完成饮食录入,目标 **单次 ≤15 秒、≤3 步**。

**功能点**
1. 三入口:拍照识别 / 语音录入 / 常吃复用。
2. 可编辑识别结果卡:食物名、份量,可点按修改。
3. 一键确认 + 乐观更新 + 撤销吐司。
4. 饮水、体重记录(轻量)。
5. 「待同步 N 条」可见入口(记录页/我的页,D-20)。

**业务逻辑/规则**
- **拍照识别(D-16)**:接入第三方食物识别 API,**异步不阻塞主流程**;返回候选食物 + 默认份量 → 用户确认/修正。**达标线:Top-1 ≥70%、Top-3 ≥90%**,用真实餐食样本上线前实测,未达标则该入口降级为「拍照→手动搜索」;手动搜索 + 常吃复用为一级兜底。
- **语音录入(D-16)**:用系统 ASR(iOS Speech framework / Android SpeechRecognizer)+ 自研轻量解析(食物库词典匹配 + 份量正则),不引入独立 NLP 服务。
- **常吃复用**:基于用户历史高频食物排序,点选即填充。
- **食物营养库(D-16)**:MVP **自建核心库**——基于《中国食物成分表》标准版 + USDA FoodData Central 公共数据清洗合并,中英双语条目,规模 ≥3000 条高频食物,覆盖每 100g 热量/蛋白/碳水/脂肪 + 别名。每条食物按份量换算后累加到当日营养(供 M4)。
- **乐观更新**:点击确认即在 UI 入账并展示「已记录·撤销」,后台异步落库;失败则回滚并提示。
- **撤销时间窗(D-11)**:「已记录·撤销」吐司展示 **10 秒**,点击撤销即撤回该条(乐观更新回滚)。
- **四态持久化与同步(D-20)**:每条记录具备四态——**提交中 / 已同步 / 待同步 / 冲突**,持久化落本地,吐司消失后仍可通过「待同步 N 条」入口确认入库状态;离线暂存、联网后按 D-20 冲突合并规则同步(见 M7)。

**用户故事与验收标准**

> US-3.1 作为忙碌用户,我希望几秒就能记完一餐。
- 给定 进入记录页,当 选择任一入口完成录入并确认,则 全流程 ≤3 步,主流程中位耗时 ≤15 秒(以埋点统计验收)。
- 给定 拍照识别返回结果,当 用户修改份量,则 营养换算实时更新。
- 给定 用户点击「确认记录」,则 立即出现「已记录·撤销」吐司;10 秒内点击撤销可撤回该条。
- 给定 识别失败/无网络,则 提供手动搜索食物兜底,不丢失已输入内容,记录进入「待同步」态并计入「待同步 N 条」。
- 给定 拍照识别实测 Top-1 <70% 或 Top-3 <90%,则 拍照入口降级为「拍照→手动搜索」流程。

**异常与边界**
- 识别置信度低 → 标记"请确认",不直接入账高风险结果。
- 离线 → 本地暂存(待同步态),联网后同步(见 M7 / D-20)。
- 重复记录 → 允许,但提供合并/删除。

**双端差异**
- **iOS**:语音识别用 Speech framework,需申请语音识别与麦克风权限。
- **Android**:语音识别用 SpeechRecognizer(API 26+ 系统能力),需麦克风权限;无网络时系统 ASR 可用性因厂商 ROM 而异,失败时回落手动搜索〔假设〕。

**埋点**:各入口点击与完成率、识别成功率(Top-1/Top-3)、单次记录耗时、撤销率、手动兜底使用率、待同步条数曝光。

---

### M4 营养可视化分析(P1)

**概述**:用红黄绿信号灯 + 一句话建议替代专业术语,让用户"看得懂、知道接下来怎么做"。

**功能点**
1. 信号灯卡片:蛋白 / 碳水 / 脂肪 / 热量,各自红黄绿 + 一句话建议。
2. 趋势图表 + 日期切换。
3. 专业数据(RDA 等)默认折叠、可展开。

**业务逻辑/规则**
- **信号灯判定(D-05,阈值待营养专业侧书面背书,开发可并行)**:以「当日累计摄入(来自 M3)÷ 当日目标(来自 M1/D-04)」的百分比判定:

| 营养素 | 绿(达标) | 黄(提醒) | 红(警示) |
|--------|-----------|-----------|-----------|
| 热量 | 85%–110% | 60%–85% 或 110%–130% | <60% 或 >130% |
| 蛋白质 | ≥90% | 70%–90% | <70%;>150% 标红(提示过量) |
| 碳水 | 85%–115% | 65%–85% 或 115%–135% | <65% 或 >135% |
| 脂肪 | 80%–110% | 55%–80% 或 110%–130% | <55% 或 >130% |

- **一句话建议(D-05)**:MVP 用**规则模板库**——按「营养素 × 落区 × 餐段」生成,中英双语各一套,不用模型生成。
- **三重编码(无障碍硬性要求)**:颜色 + 图标 + 文字,绝不单靠颜色(承接设计稿 / 无障碍约束,见 M8)。
- 阈值与 TDEE 公式取得营养专业侧书面背书后方可上线 M4(评审项 3,为 M4 上线 Gate,不阻塞 MVP M1–M3 开发)。

**用户故事与验收标准**

> US-4.1 作为不懂营养的用户,我希望一眼知道今天吃得好不好、该补什么。
- 给定 当日已有记录,当 进入数据页,则 展示四类信号灯卡片,每卡含 颜色+图标+文字 三重编码 + 规则模板库生成的一句话建议。
- 给定 某营养素按 D-05 阈值落入红区,则 该卡显示红灯并给出可执行建议。
- 给定 用户下滑展开,则 显示专业数值(目标值/已摄入/RDA),默认折叠。

**异常与边界**:当日无记录 → 显示空状态引导去记录,不显示误导性信号灯。

**埋点**:数据页曝光、信号灯卡点击、建议曝光、专业数据展开率、趋势图查看。

---

### M5 社区与激励(P1 基础 / P2 进阶)

**概述**:用连续打卡成就、好友挑战、轻量互动对抗中期动力衰减。

**功能点**
- **P1**:打卡发布与打卡流(单列卡片);连续天数徽章(3/7/30 天里程碑);轻互动(点赞/鼓励)。
- **P2**:挑战赛(报名/排行);好友关系与互相监督。

**业务逻辑/规则**
- **连续天数(streak)口径(D-12,已强制二选一拍板)**:**以「断食打卡达标」(D-08)为唯一 streak 口径**,不与饮食记录并行计数;饮食记录另有独立的「记录天数」统计,仅展示、不进 streak。
- **中断与补签卡(D-12)**:
  - 中断 → 当前 streak 归零;断签弹窗同时展示「连胜如何计算」与补签卡状态(可补签 / 已用尽 / 已断签三态)。
  - **补签卡规则表**:

| 规则项 | 取值 |
|--------|------|
| 发放 | 每月 1 日发放 2 张 |
| 有效期 | 当月有效,月底清零 |
| 可补范围 | 仅最近 7 天内的断签日 |
| 库存上限 | 2 张(不累积) |

  - streak 状态机与完整规则表详见《规格-M5 streak 与补签卡》。
- 里程碑(3/7/30)触发成就徽章与庆祝动效(承接设计稿微交互)。
- **内容安全(D-17)**:打卡图文接第三方内容安全 API(阿里云或腾讯云内容安全,M0 定),**文本/图片先审后发**,审核异常转人工队列;另提供举报机制。

**用户故事与验收标准**

> US-5.1 作为需要外部激励的用户,我希望坚持被看见、被鼓励。
- 给定 用户当日断食打卡达标,当 连续达到 7 天,则 展示「7 天连胜」徽章与可分享图卡。
- 给定 用户发布打卡,当 内容审核通过,则 出现在打卡流,他人可轻互动;审核未过或异常则不发布并提示(先审后发)。
- 给定 中断一天,则 当前 streak 归零,弹窗同时展示「连胜如何计算」与补签卡三态;用户持有效补签卡且断签日在最近 7 天内时可补签恢复。

**异常与边界**:违规内容举报后下架;空打卡流显示引导内容;补签卡已用尽/断签超 7 天时弹窗明确告知不可补。

**埋点**:打卡发布、互动率、徽章触达、分享、挑战赛报名/完成、补签卡使用。

---

### M6 数据趋势与深度报告(P2)

**概述**:体重/营养/断食的趋势可视化与周期报告,服务长期留存。
- 趋势图(体重、热量、断食时长)、7 天/30 天成长轨迹(承接设计稿信息图)。
- 周期报告(周报/月报)〔假设〕。
- 验收:数据准确、可按维度切换、空数据有引导。

---

### M7 账号、数据与设置(P0 基础)

**概述**:账号体系、数据存储/同步、隐私、设置。
- **账号登录方式(D-13)**:国内 MVP 支持 **手机号验证码(主)+ 微信登录 + Sign in with Apple**(iOS 上架强制要求提供 Apple 登录);Google 登录随英文版海外上架评估(V1.1+),MVP 不做。
- **数据同步策略(D-20)**:本地优先(offline-first)+ 云端同步,离线可用、联网合并,支撑 M3 乐观更新与离线记录:
  - 记录四态持久化:**提交中 / 已同步 / 待同步 / 冲突**。
  - 冲突合并:字段级 LWW(Last-Write-Win,以服务端时间戳为准)+ 冲突队列;不可合并字段(如已删除又修改)保留双份并人工提示。
  - 幂等:客户端生成 `clientRequestId`,服务端去重。
  - 记录页/我的页提供「待同步 N 条」可见入口。
  - 详见《规格-数据同步与四态持久化》。
- **隐私(D-18,法务 M0 介入,待外部确认)**:健康数据为敏感个人信息,需最小化收集、明示授权(首次启动隐私弹窗)、用户可导出/删除(设置内入口)、全程「非医疗建议」免责声明(中英双语)、SDK 与权限清单公示;详见《合规-隐私与合规方案》。
- **设置**:方案、目标、提醒(提前量与文案)、**语言(跟随系统/简体中文/English,见 D-15)**、主题(亮/暗/跟随系统)、**无障碍开关**。

**双端差异**
- **iOS**:必须提供 Sign in with Apple(App Store 上架强制);推送走 APNs。
- **Android**:无 Apple 登录;推送走 FCM,国内厂商通道经聚合推送 SDK(个推或极光,M0 定,D-17)。

---

### M8 无障碍与包容性(P0,贯穿全局,硬性)

> 这是设计稿的强约束,在 PRD 中作为**全局非功能性硬性需求 + 验收门槛**,任何模块不达标不可上线。

- 对比度满足 WCAG 2.1 AA(≥4.5:1)。
- 信号灯等状态信息**三重编码**(颜色+图标+文字),绝不单靠颜色。
- 交互元素 ≥44px,语音按钮 ≥48px;支持系统字号放大至 200%,正文最小 14px。
- 完整无障碍标签(语义化标记,Flutter Semantics)、键盘/读屏可达(iOS VoiceOver / Android TalkBack)、统一焦点态。
- 尊重系统减弱动效设置(iOS「减弱动态效果」/ Android「移除动画」,对应 `prefers-reduced-motion` 语义),可关闭非必要动效。
- 图像素材遵循设计稿第七节的包容性与光照公平准则及 7 项 QA 清单。

---

## 五、数据模型(关键实体,供技术参考)〔假设,需技术评审细化〕

| 实体 | 关键字段 | 说明 |
|------|----------|------|
| User | id, 基础信息(性别/年龄/身高/体重/活动量), 目标, 无障碍偏好, 主题偏好, **语言偏好(language: zh-CN / en / 跟随系统,D-15)** | 个人资料 |
| FastingPlan | id, userId, 断食法类型, 进食窗口(start/end), 生效日期(次日 0 点生效,D-06) | 用户当前方案 |
| FastingRecord | id, userId, date(归属日=进食窗口所属自然日,D-07), 实际断食时长, 是否达标(D-08), 状态变更日志, 时间戳 UTC 存储 | 每日断食记录 |
| FoodEntry | id, userId, datetime(UTC), foodId, 份量, 录入方式(拍照/语音/常吃/手动), 营养快照, **同步状态(提交中/已同步/待同步/冲突,D-20), clientRequestId(幂等,D-20)** | 单条饮食记录 |
| Food(食物库) | id, 名称(中英双语 + 别名,D-15/D-16), 每100g 热量/蛋白/碳水/脂肪, 别名 | 营养基准库(MVP 自建,D-16) |
| DailyNutrition | userId, date, 累计四大营养, 信号灯结果(按 D-05 阈值) | 由 FoodEntry 聚合 |
| Streak | userId, 当前连续天数(唯一口径=断食达标,D-12), 历史最长, 里程碑状态, **补签卡字段(库存数量/当月已用/最近断签日,D-12)** | 激励 |
| Post(打卡) | id, userId, 图文, 连续天数, 互动计数, 审核状态(先审后发,D-17) | 社区 |

---

## 六、需求—流失点—设计映射(可追溯)

| 用户流失点 | 对应功能(本 PRD) | 对应设计(设计稿) |
|------------|-------------------|--------------------|
| 启动期门槛高 | M1 新手引导与方案库 | 3 题问卷引导页、方案库 |
| 记录期(最高频流失) | M3 快捷饮食记录 | 三入口、≤15 秒、乐观更新 |
| 反馈期看不懂 | M4 营养可视化分析 | 红黄绿信号灯 + 一句话建议 |
| 维持期动力衰减 | M2 断食提醒 + M5 社区激励 | 锁屏小组件、连胜徽章、挑战赛 |
| 全程包容性 | M8 无障碍 | WCAG AA、三重编码、字号放大 |

---

## 七、非功能需求

| 类别 | 要求 |
|------|------|
| 性能 | 关键样式内联;首页计时与记录入口优先渲染;图片懒加载;交互反馈 <100ms(承接设计稿) |
| 无障碍 | 见 M8(硬性门槛) |
| 兼容性 | **iOS 15+ / Android 8.0(API 26)+**(D-14);Live Activity 为 iOS 16.1+ 增强项、低版本降级普通 Widget;mobile-first,断点 sm360/md768/lg1024 |
| 隐私合规 | 健康数据为敏感个人信息:最小化收集、明示授权、可导出/删除、合规存储;适用《个人信息保护法》+ 英文版 GDPR 边界评估(D-18,待法务确认,见《合规-隐私与合规方案》) |
| 可用性 | 离线可记录、联网同步(D-20 四态);乐观更新失败可回滚 |
| 国际化 | **MVP 即支持简体中文 + 英文双语(D-15,替换 v1.0「MVP 仅中文」假设)**:默认跟随系统语言,设置内可手动切换;文案全部走 i18n key 管理,禁止硬编码。范围:UI 文案、信号灯建议、推送/本地通知、趣味文案、食物库条目(中英双语名称+别名)、日期/时间/数字格式本地化;社区 UGC MVP 不分语言圈全量混排,V1.1 再评估。详细规范见《规格-i18n 中英双语》 |

---

## 八、依赖与技术前置(已按 D-16 / D-17 完成选型)

| 依赖 | 选型结论 | 影响模块 |
|------|----------|----------|
| 食物营养数据库 | **自建核心库**:《中国食物成分表》标准版 + USDA FoodData Central 清洗合并,中英双语,≥3000 条(D-16) | M3 / M4 |
| 图像识别服务(拍照识别) | **第三方食物识别 API**,达标线 Top-1 ≥70% / Top-3 ≥90%,真实样本上线前实测,未达标降级「拍照→手动搜索」(D-16) | M3 |
| 语音识别(ASR)+ 份量解析 | **系统 ASR**(iOS Speech framework / Android SpeechRecognizer)+ 自研轻量解析(词典匹配 + 份量正则),不引入独立 NLP 服务(D-16) | M3 |
| 客户端技术栈 | **Flutter(稳定版 3.x)单代码库双端**;Riverpod / SQLite(drift)/ dio / slang+intl / flutter_local_notifications;小组件 iOS WidgetKit(+Live Activity)/ Android AppWidget 经 `home_widget` + 原生桥(D-17) | 全局 |
| 后端与推送 | Node.js(NestJS)+ PostgreSQL + Redis,REST API;APNs + FCM,国内 Android 厂商通道经聚合推送 SDK(个推或极光,M0 定)(D-17) | M2 / M7 |
| 锁屏小组件能力 | iOS WidgetKit(Live Activity 需 iOS 16.1+,降级普通 Widget)/ Android AppWidget(D-14 / D-17) | M2 |
| 内容审核能力 | **第三方内容安全 API**(阿里云或腾讯云,M0 定),文本/图片先审后发,异常转人工队列(D-17) | M5 |
| 账号与云同步基础设施 | 手机号验证码 + 微信 + Sign in with Apple;自建同步服务(四态 + LWW + 幂等,D-13 / D-20) | M7 |

---

## 九、里程碑(建议,需排期确认)〔假设〕

| 阶段 | 内容 |
|------|------|
| M0 | PRD v1.1 定稿 + 计时逻辑技术 Spike(D-07,第 1 周)+ 聚合推送 SDK 与内容安全供应商定商(D-17)+ 法务前置介入(D-18)+ TDEE 公式与红黄绿阈值营养侧书面背书(D-04/D-05,M4 上线 Gate) |
| M1 | MVP 开发(M1/M2/M3/M7/M8)→ 灰度 |
| M2 | V1.1(M4/M5 基础)|
| M3 | V1.2(M5 进阶 / M6)|

---

## 十、风险

| 风险 | 影响 | 应对 |
|------|------|------|
| 食物识别准确率不足 | 记录摩擦不降反升 | 已设达标线 Top-1 ≥70%/Top-3 ≥90% 并实测(D-16);必做手动搜索兜底 + 常吃复用;识别仅作加速、异步不阻塞 |
| 营养阈值不科学 | 误导用户,合规风险 | D-04/D-05 阈值走热配置;营养专业侧书面背书为 M4 上线 Gate;声明非医疗建议 |
| 断食跨天/时区计时错乱 | 核心功能不可信 | UTC 存储 + 归属日规则已拍板(D-07);M0 第一周计时 Spike + 状态机迁移表验收(评审项 1) |
| 健康数据合规 | 法律风险 | 法务 M0 前置介入(D-18),最小化收集,PIPL + GDPR 边界评估 |
| 离线同步冲突丢失数据 | 用户不信任记录链路 | 四态持久化 + 字段级 LWW + 幂等(D-20),「待同步 N 条」可见入口 |

---

## 十一、术语表

- **断食窗口 / 进食窗口**:不进食 / 允许进食的时间段。
- **打卡归属日**:一次断食计入的自然日,固定为其后进食窗口所属自然日(D-07)。
- **破窗容差**:手动提前结束断食仍记达标的最长提前量,15 分钟(D-08)。
- **信号灯**:绿=达标、黄=适量、红=超标的三色营养反馈,强制三重编码(阈值见 D-05)。
- **连续天数(Streak)**:连续断食打卡达标的天数,唯一口径见 D-12。
- **补签卡**:每月发放 2 张、可补最近 7 天断签日的恢复道具(D-12)。
- **乐观更新**:UI 先入账、后台异步落库,失败回滚;撤销窗 10 秒(D-11)。
- **四态持久化**:记录的 提交中/已同步/待同步/冲突 四种同步状态(D-20)。
- **TDEE**:每日总能量消耗,用于估算营养目标(公式见 D-04)。

---

## 十二、开放问题汇总(v1.0 全部关闭)

> v1.0 本章 10 项开放问题已在《明食 · EatWise — 开放问题决策记录》v1.0 中逐条拍板(D-01～D-20),状态如下;仍开放项见文末。

| # | v1.0 开放问题 | 结论 | 对应决策 |
|---|----------------|------|----------|
| 1 | 北极星指标与各项 Success Metrics 目标值口径 | 已决策:WAU-Active + 有效记录日定义,1.4 节目标值确认为验收口径 | D-01 |
| 2 | M1:问卷题目维度、方案推荐规则表、TDEE 公式与营养素配比 | 已决策:问卷 3 题、推荐规则表已定;TDEE 公式已定、待营养背书 | D-02 / D-03 / D-04 |
| 3 | M1:方案更换的生效时机 | 已决策:次日 0 点(本地时区)生效 | D-06 |
| 4 | M2:打卡归属日、破窗容差、提醒提前量、延长步进与上限、时区/改时间处理 | 已决策:归属日=进食窗口所属自然日;容差 15 分钟;提前 15 分钟提醒;延长步进 30min/上限 4h;UTC 存储,状态机规格 M0 Spike 验证 | D-07 / D-08 / D-09 / D-10 |
| 5 | M3:食物营养库来源、识别与 ASR/NLP 选型、撤销时间窗 | 已决策:食物库自建;识别接第三方 API(达标线 Top-1≥70%/Top-3≥90%);ASR 用系统能力;撤销窗 10 秒 | D-11 / D-16 |
| 6 | M4:红黄绿阈值区间、一句话建议生成方式 | 已决策:阈值表已定(待营养背书);建议用规则模板库(中英双语) | D-05 |
| 7 | M5:streak 口径、补签卡、内容审核方案 | 已决策:断食达标为唯一口径;补签卡规则已定;先审后发接第三方内容安全 API | D-12 / D-17 |
| 8 | M7:账号登录方式、数据同步与隐私合规 | 已决策:手机号+微信+Apple 登录;同步四态 + LWW + 幂等;合规方案待法务确认 | D-13 / D-20 / D-18 |
| 9 | 兼容性:支持的 iOS/Android 最低版本 | 已决策:iOS 15+ / Android 8.0(API 26)+ | D-14 |
| 10 | 是否纳入可穿戴/硬件数据接入 | 已决策:MVP 与 V1.1 均不做,V1.2 后再评估 | D-19 |

**仍开放项**
1. **海外上架与 GDPR 评估时机**:MVP 上架区域为中国区 + 英文界面可用;海外上架待 GDPR 评估完成(D-18)后另行决策〔待外部确认:法务〕,Google 登录随之上架评估(D-13/D-15)。
2. TDEE 公式与红黄绿阈值的营养专业侧书面背书(D-04/D-05)〔待外部确认:M4 上线 Gate〕。
3. 聚合推送 SDK(个推/极光)与内容安全供应商(阿里云/腾讯云)M0 定商(D-17)〔假设排期:M0 内关闭〕。

---

> **附:与设计文档的关系**
> 本 PRD 关注需求与逻辑,呈现层(色彩、字体、组件、Token、微交互、视觉/出图)以《产品设计方案定稿》为准,两者配套使用:**PRD 定"做什么",设计稿定"长什么样",共同交付开发。**
